# Copy to .env and fill values. Do not commit .env (it is gitignored).

# Server
PORT=5000

# Delta Exchange API (server) – optional; default is India API
DELTA_BASE_URL=https://api.india.delta.exchange

# Delta Exchange API keys – server uses these on start (balance, fills, positions)
DELTA_API_KEY=
DELTA_SECRET_KEY=


