Build a fully functional personal crypto auto-trading dashboard integrated with Delta Exchange API (single-user only).

All pages must be fully working. If real API data is unavailable, use realistic dummy data so that the system appears completely operational.

The platform must look and behave like a professional crypto futures trading terminal.

UI DESIGN REQUIREMENTS

Use a premium black-based trading theme:

Main Background: #000000
Card Background: #111111
Secondary Card: #161616
Primary Accent (Orange): #F97316
Profit Color: #22C55E
Loss Color: #EF4444
Primary Text: #FFFFFF
Muted Text: #9CA3AF
Border Color: rgba(255,255,255,0.06)

Design Style:

Glassmorphism dark theme

Subtle hover animations

Soft glow effect on active buttons

Clean trading terminal layout

Fully responsive (desktop and mobile)

ARCHITECTURE

Frontend:

Next.js or React

Tailwind CSS

WebSocket for real-time UI updates

All pages must be navigable and functional

Backend:

Python (FastAPI recommended)

Modular trading engine structure

Delta Exchange API integration ready

Use realistic dummy market data if API not connected

REQUIRED PAGES (ALL MUST WORK)

Dashboard Page

Live price ticker (dummy allowed if needed)

TradingView-style chart container

Account balance card

Current open position card

Real-time PNL display

Bot status indicator (Running / Stopped / Waiting)

Performance Metrics Section:

Total trades

Win rate

ROI

Profit factor

Max drawdown

Bot Control Page

Symbol selector (BTCUSDT, ETHUSDT, etc.)

Timeframe selector (1m, 5m, 15m, 1h)

Strategy Selector Options:

EMA crossover

RSI strategy

Breakout strategy

Auto detect (ADX-based detection)

Risk Settings:

Risk percentage per trade input

Maximum daily loss input

Leverage selector

Controls:

Start Bot button

Stop Bot button

Changing configuration must update the bot state in real time.

Trade History Page

Table must include:

Trade ID

Entry price

Exit price

Side (Buy/Sell)

PNL

Status

Use realistic dummy trade data if real data is unavailable

Include pagination

Settings Page

Delta API Key input field

Secret Key input field

Save button

Secure storage logic placeholder

Connection status message (Connected / Not Connected)

BACKEND REQUIREMENTS

Python FastAPI backend structured as:

main.py

websocket_manager.py

strategy_engine.py

risk_manager.py

order_executor.py

position_monitor.py

Required Features:

Fetch candles (real or simulated)

Indicator calculations (EMA, RSI, ADX)

Signal generation logic

Position size calculation using formula:
account_balance × risk_percent

Automatic stop loss placement

Automatic take profit placement

Daily loss protection logic

Logging system

Continuous bot execution loop

If Delta API is not connected, simulate market movement so the system behaves realistically.

SYSTEM BEHAVIOR REQUIREMENTS

Bot must run independently once started

UI must reflect real-time bot state

Switching strategy must update internal trading logic

If maximum daily loss is reached, bot must automatically stop

All buttons must function visually and logically